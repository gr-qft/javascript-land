# The problem

In this section we build another fullstack app. We'll set up a
server serving a frontend that displays a set of book titles and
some excerpts. We proceed like the last tutorial, where we setup
a backend and then the frontend.
